<h2>Categories <%= @categories.length %> </h2>

<ol>
    <% @categories.each do |c| %>
        <li>
            <%= link_to category_products_path(c) do %>
                <%= c.title %>
                (<%= c.products_count %>)
            <% end %>
        </li>
    <% end %>
</ol>